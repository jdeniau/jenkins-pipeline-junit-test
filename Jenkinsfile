#!groovy
pipeline {
    agent any

    stages {
        stage('get github PR description') {
            steps {
                script {
                  // withChecks('Junit') {
                    junit 'junit.xml'
                  // }
                }
            }
        }
    }
}
                  
